# Flow Map: Image Generation

> Auto-generated by `node scripts/tools/flow-map.js pipeline generate_image`
> Scanned: static analysis (regex/heuristic) — not runtime traced.

## WebSocket Round-Trip

```mermaid
sequenceDiagram
    participant UI as Client UI
    participant WS as websocket.js
    participant SRV as Server Handler
    participant IMPL as Handler Impl
    UI->>WS: generateImage()
    WS->>SRV: generate_image
    Note over SRV: modules/ws/handlers/60-generationHandler.js:35
    SRV->>IMPL: handleImageGeneration()
    IMPL-->>WS: image_generation_progress
    WS-->>UI: public/scripts/ws/handlers/50-generationInbound.js ((inline))
    IMPL-->>WS: image_generation_response
    WS-->>UI: public/scripts/ws/handlers/100-imageResponseInbound.js ((inline))
    IMPL-->>WS: image_generation_error
    WS-->>UI: public/scripts/ws/handlers/50-generationInbound.js ((inline))
```

## Server Handler

| Field | Value |
| --- | --- |
| Packet | `generate_image` |
| Owner | `generation` |
| File | `modules/ws/handlers/60-generationHandler.js:35` |
| Function | `handleImageGeneration` |
| Impl | `modules/ws/handlers/generationImpl.js` |

## Client Callers

| File | Function | Line |
| --- | --- | --- |
| `public/scripts/websocket.js` | `generateImage` | 5009 |

## Client Inbound Handlers

| Type | File | Handler | Phase |
| --- | --- | --- | --- |
| `image_generation_response` | `public/scripts/ws/handlers/100-imageResponseInbound.js` | `(inline)` | only |
| `image_generation_progress` | `public/scripts/ws/handlers/50-generationInbound.js` | `(inline)` | only |
| `image_generation_error` | `public/scripts/ws/handlers/50-generationInbound.js` | `(inline)` | only |

## Server Impl Call Graph

```mermaid
flowchart TD
    subgraph generationImpl_js ["generationImpl.js"]
        normalizeExpansionOverrideParams["normalizeExpansionOverrideParams"]
        handleImageGeneration["handleImageGeneration"]
        handleImageReroll["handleImageReroll"]
        handleImageUpscaling["handleImageUpscaling"]
        handlePreviewExpandImagePrompt["handlePreviewExpandImagePrompt"]
        handleImageExpansion["handleImageExpansion"]
        handleImageExpansionReroll["handleImageExpansionReroll"]
        handleCancelGeneration["handleCancelGeneration"]
        handleDynamicGenerationProgress["handleDynamicGenerationProgress"]
        handleResolveDynamicContext["handleResolveDynamicContext"]
        handleCompileDynamicGeneration["handleCompileDynamicGeneration"]
        handleApplyTendaiPreview["handleApplyTendaiPreview"]
        handleResolveTextReplacements["handleResolveTextReplacements"]
        handlePreviewExpandImagePrompt --> normalizeExpansionOverrideParams
        handleImageExpansion --> normalizeExpansionOverrideParams
        handleImageExpansionReroll --> normalizeExpansionOverrideParams
    end
```

## Limitations

- Static scan only; dynamic `sendMessage(type)` and runtime-registered handlers may be missed.
- Cross-file JavaScript calls are not fully resolved.
- Re-run `pnpm flow-map pipeline <name>` after handler changes.
