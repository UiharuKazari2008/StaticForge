data/
images/
.previews/
node_modules/
.git-audit.log
*.swp
*.bak
.env
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
config.json
*config.json
.cache/*
# Legacy SQLite snapshot dir (before unified .cache/checkpoints/<dbStem>/); safe to keep for old trees
.checkpoints/*
logs/
log.txt
securePrompts/*
tmp/
.agents/skills/*
.cursor/debug-*
# Nested clone of DreamScape/cursor-rules (cron-pulled on host)
.cursor/rules/
graphify-out/
**/graphify-out/
nax_tags.json
danbooru_tagwiki.json
danbooru_tagwiki_merged.json
e621_wiki_fixed.json
danbooru_tag_groups_hierarchy.json
tools/
# Per-agent busy locks (never commit)
.agent-*
# Local host sync remote (see scripts/dreamscape-sync.env.example)
dreamscape-sync.env
# Chrome for Testing binaries (setup-dump-deps.sh --chrome-for-testing)
.cache/chrome-for-testing/
tools/chrome-for-testing/
